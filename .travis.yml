sudo: false
install: true
language:
  - erlang

env:
  global:
    - TRAVIS_CI=true
    
services:
  - redis-server

#addons:
#  apt:
#    packages:
#      - lynx

#before_install:
#  - ./rebar3 dialyzer

script:
  - make build #skip automated regression tests
  - make ck

notifications:
  email:
    - shuieryin@gmail.com

otp_release:
  - 19.2
